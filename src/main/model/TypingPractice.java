package model;

public class TypingPractice {
    private double wpm;
    private double accuracy;
    private boolean timed;      // true if timed, false if word-count
    private String focus;       // punctuation, numbers, everything, etc.

    public TypingPractice(String focus, boolean timed) {
        // stub
    }
}
